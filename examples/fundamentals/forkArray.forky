var size = 3;
var arr[size][size] = 0;

fork arr i, e {
    fork e j, f {
        set arr[i][j] = i + j + f;
        print("Row: ' + i + ", Col: ' + j + ", Value: ' + arr[i][j]);
    }
}

print("Final array:');
print(arr);

fork arr {
    print("Processing array element');
}

