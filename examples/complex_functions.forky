func max(a, b, c) {
    if (a >= b and a >= c) {
        return a;
    } else if (b >= a and b >= c) {
        return b;
    } else {
        return c;
    }
}

sum = add(10, add(2, 3) * 2);
print(sum);

pow_result = power(2 + 1, 3 - 1);
print(pow_result);

max_val = max(7, 15 - 3, add(5, 8));
print(max_val);